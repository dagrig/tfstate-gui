import { Routes } from '@angular/router';
import { StateViewerComponent } from '../state-viewer/state-viewer.component';

export const routes: Routes = [
  { path: '', component: StateViewerComponent },
  // Add other routes as needed
];